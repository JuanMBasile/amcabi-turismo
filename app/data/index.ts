/**
 * AMCABI Turismo - Static Fallback Data Index
 *
 * Central export point for all fallback data.
 * Use these when Sanity CMS is not configured.
 *
 * @example
 * import { destinations, departures, promos } from '@/app/data'
 */

export { destinations } from './destinations'
export { departures } from './departures'
export { promos } from './promos'
export { services } from './services'
export { faqs } from './faqs'
